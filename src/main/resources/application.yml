spring:
  application:
    name: library-management-system
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/library_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    # Existing DBs created via ddl-auto: baseline marks V1 as applied without re-running
    baseline-on-migrate: true
    baseline-version: 1

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 100

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info
      base-path: /actuator
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when_authorized
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET:library-management-jwt-secret-key-min-32-chars}
      expiration-ms: ${JWT_EXPIRATION_MS:3600000}
  auth:
    rate-limit:
      login-register-max: ${AUTH_RATE_LIMIT_MAX:10}
      window-seconds: ${AUTH_RATE_LIMIT_WINDOW_SEC:60}
    refresh-token:
      expiration-days: ${AUTH_REFRESH_EXPIRATION_DAYS:7}
    password-reset:
      expiration-minutes: ${AUTH_PASSWORD_RESET_EXPIRATION_MIN:60}
      expose-token-in-response: ${AUTH_EXPOSE_RESET_TOKEN:false}

---
spring:
  config:
    activate:
      on-profile: dev

application:
  auth:
    password-reset:
      expose-token-in-response: true

logging:
  level:
    root: INFO
    org.springframework.security: INFO
    com.library.management: INFO
